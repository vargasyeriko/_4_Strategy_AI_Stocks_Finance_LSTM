# Notebooks & archived ALFA assets

| Path | Contents |
|------|----------|
| `notebooks/` (this tree) | Jupyter work, checkpoints, old `save/` experiments, `arch/`, runtime data dirs used by pre–`src_3` scripts |
| `alfa_scripts/` | Former root `*.py` files — **not** used by `auto.py` |
| `runtime/` | `_1_fetch/`, ledger folders, `save/`, `arch/`, etc. |
| `pie_out_old/` | Old pie-chart output tree |
| `media/` | PDFs / images from earlier runs |

**Supported pipeline:** `../auto.py` → `src_3/run.py` only.
